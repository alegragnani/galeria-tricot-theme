{%- comment -%}
  Cart Drawer (mini cart) — Galeria Tricot (estilo BP)
  100% AJAX — não recarrega a página ao adicionar/remover/mudar quantidade
{%- endcomment -%}

<div class="cart-drawer__overlay" id="cart-overlay"></div>

<aside class="cart-drawer" id="cart-drawer">
  <div class="cart-drawer__header">
    <h2 class="cart-drawer__title">MINHA SACOLA</h2>
    <button class="cart-drawer__close" id="cart-drawer-close" aria-label="Fechar sacola">
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
  </div>

  <div class="cart-drawer__items" id="cart-drawer-items">
    {%- comment -%} Conteúdo carregado via JS {%- endcomment -%}
  </div>

  <div class="cart-drawer__footer" id="cart-drawer-footer" style="display:none;">
    <div class="cart-drawer__coupon" id="cart-coupon-section">
      <div class="cart-drawer__coupon-row">
        <input type="text" id="cart-coupon-input" class="cart-drawer__coupon-input" placeholder="Cupom de desconto" autocomplete="off">
        <button type="button" id="cart-coupon-btn" class="cart-drawer__coupon-btn" onclick="applyCoupon()">APLICAR</button>
      </div>
      <p class="cart-drawer__coupon-msg" id="cart-coupon-msg"></p>
    </div>
    <div class="cart-drawer__subtotal">
      <span>SUBTOTAL</span>
      <span id="cart-drawer-total"></span>
    </div>
    <p class="cart-drawer__shipping-note">Frete calculado no checkout</p>
    <a href="/checkout" class="cart-drawer__checkout-btn">FINALIZAR COMPRA</a>
  </div>
</aside>

<style>
  .cart-drawer__overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.4);
    z-index: 10001;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.35s ease, visibility 0.35s ease;
  }
  .cart-drawer__overlay.is-open {
    opacity: 1;
    visibility: visible;
  }

  .cart-drawer {
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    width: 420px;
    max-width: 90vw;
    background: #ffffff;
    z-index: 10002;
    display: flex;
    flex-direction: column;
    transform: translateX(100%);
    transition: transform 0.35s ease;
    box-shadow: -4px 0 20px rgba(0,0,0,0.08);
  }
  .cart-drawer.is-open {
    transform: translateX(0);
  }

  .cart-drawer__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 24px;
    border-bottom: 1px solid #f0ede8;
  }
  .cart-drawer__title {
    font-family: 'Raleway', sans-serif;
    font-size: 16px;
    font-weight: 600;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: #121212;
    margin: 0;
  }
  .cart-drawer__close {
    background: none;
    border: none;
    cursor: pointer;
    color: #545454;
    padding: 4px;
  }
  .cart-drawer__close:hover { opacity: 0.6; }

  .cart-drawer__items {
    flex: 1;
    overflow-y: auto;
    padding: 0 24px;
  }

  /* ---- Item do carrinho ---- */
  .cart-drawer-item {
    display: flex;
    gap: 16px;
    padding: 20px 0;
    border-bottom: 1px solid #f0ede8;
  }
  .cart-drawer-item__img {
    width: 90px;
    height: 120px;
    object-fit: cover;
    flex-shrink: 0;
  }
  .cart-drawer-item__info {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .cart-drawer-item__vendor {
    font-family: 'Raleway', sans-serif;
    font-size: 11px;
    font-weight: 400;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    color: #aaaaaa;
  }
  .cart-drawer-item__title {
    font-family: 'Raleway', sans-serif;
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    color: #121212;
    text-decoration: none;
    line-height: 1.4;
  }
  .cart-drawer-item__title:hover { opacity: 0.7; }
  .cart-drawer-item__price {
    font-family: 'Raleway', sans-serif;
    font-size: 14px;
    font-weight: 500;
    color: #545454;
  }
  .cart-drawer-item__variant {
    font-family: 'Raleway', sans-serif;
    font-size: 12px;
    font-weight: 400;
    color: #aaaaaa;
    text-transform: uppercase;
    letter-spacing: 0.3px;
  }
  .cart-drawer-item__qty {
    display: flex;
    align-items: center;
    gap: 0;
    margin-top: 8px;
  }
  .cart-drawer-item__qty-btn {
    width: 32px;
    height: 32px;
    border: 1px solid #e0ddd8;
    background: none;
    cursor: pointer;
    font-size: 16px;
    color: #545454;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Raleway', sans-serif;
  }
  .cart-drawer-item__qty-btn:hover { border-color: #545454; }
  .cart-drawer-item__qty-val {
    width: 36px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Raleway', sans-serif;
    font-size: 14px;
    font-weight: 500;
    color: #121212;
    border-top: 1px solid #e0ddd8;
    border-bottom: 1px solid #e0ddd8;
  }
  .cart-drawer-item__remove {
    background: none;
    border: none;
    cursor: pointer;
    color: #aaaaaa;
    padding: 4px;
    align-self: flex-start;
    flex-shrink: 0;
  }
  .cart-drawer-item__remove:hover { color: #545454; }

  /* ---- Empty ---- */
  .cart-drawer__empty {
    text-align: center;
    padding: 60px 20px;
  }
  .cart-drawer__empty-text {
    font-family: 'Raleway', sans-serif;
    font-size: 14px;
    color: #aaaaaa;
    margin: 0 0 20px 0;
  }
  .cart-drawer__empty-link {
    font-family: 'Raleway', sans-serif;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.8px;
    text-transform: uppercase;
    color: #545454;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  /* ---- Footer ---- */
  .cart-drawer__footer {
    padding: 20px 24px;
    border-top: 1px solid #f0ede8;
  }

  /* Coupon */
  .cart-drawer__coupon {
    margin-bottom: 16px;
  }
  .cart-drawer__coupon-row {
    display: flex;
    gap: 8px;
  }
  .cart-drawer__coupon-input {
    flex: 1;
    padding: 10px 12px;
    border: 1px solid #e0ddd8;
    font-family: 'Raleway', sans-serif;
    font-size: 13px;
    color: #121212;
    outline: none;
    transition: border-color 0.2s;
  }
  .cart-drawer__coupon-input::placeholder {
    color: #aaaaaa;
    text-transform: none;
  }
  .cart-drawer__coupon-input:focus {
    border-color: #B3883D;
  }
  .cart-drawer__coupon-btn {
    padding: 10px 16px;
    background: none;
    border: 1px solid #121212;
    font-family: 'Raleway', sans-serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.8px;
    text-transform: uppercase;
    color: #121212;
    cursor: pointer;
    transition: background 0.2s, color 0.2s;
    white-space: nowrap;
  }
  .cart-drawer__coupon-btn:hover {
    background: #121212;
    color: #ffffff;
  }
  .cart-drawer__coupon-msg {
    font-family: 'Raleway', sans-serif;
    font-size: 12px;
    margin: 6px 0 0 0;
    min-height: 16px;
  }
  .cart-drawer__coupon-msg.success { color: #2e7d32; }
  .cart-drawer__coupon-msg.error { color: #c62828; }

  .cart-drawer__subtotal {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
  }
  .cart-drawer__subtotal span {
    font-family: 'Raleway', sans-serif;
    font-size: 14px;
    font-weight: 600;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: #121212;
  }
  .cart-drawer__shipping-note {
    font-family: 'Raleway', sans-serif;
    font-size: 12px;
    color: #aaaaaa;
    text-align: center;
    margin: 0 0 16px 0;
  }
  .cart-drawer__checkout-btn {
    display: block;
    width: 100%;
    padding: 16px;
    background: #B3883D;
    color: #ffffff;
    text-align: center;
    font-family: 'Raleway', sans-serif;
    font-size: 14px;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: opacity 0.2s;
  }
  .cart-drawer__checkout-btn:hover { opacity: 0.85; }
</style>

<script>
(function() {
  var drawer = document.getElementById('cart-drawer');
  var overlay = document.getElementById('cart-overlay');
  var closeBtn = document.getElementById('cart-drawer-close');
  var itemsContainer = document.getElementById('cart-drawer-items');
  var footer = document.getElementById('cart-drawer-footer');
  var totalEl = document.getElementById('cart-drawer-total');

  function fmt(cents) {
    return 'R$ ' + (cents / 100).toLocaleString('pt-BR', { minimumFractionDigits: 2 });
  }

  window.openCartDrawer = function() {
    renderCart();
    drawer.classList.add('is-open');
    overlay.classList.add('is-open');
    document.body.style.overflow = 'hidden';
  };

  window.closeCartDrawer = function() {
    drawer.classList.remove('is-open');
    overlay.classList.remove('is-open');
    document.body.style.overflow = '';
  };

  overlay.addEventListener('click', closeCartDrawer);
  closeBtn.addEventListener('click', closeCartDrawer);
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape' && drawer.classList.contains('is-open')) closeCartDrawer();
  });

  function renderCart() {
    fetch('/cart.js')
      .then(function(r) { return r.json(); })
      .then(function(cart) {
        if (cart.item_count === 0) {
          itemsContainer.innerHTML = '<div class="cart-drawer__empty"><p class="cart-drawer__empty-text">Sua sacola está vazia</p><a href="/collections/all" class="cart-drawer__empty-link">Continuar comprando</a></div>';
          footer.style.display = 'none';
          updateHeaderCount(0);
          return;
        }

        var html = '';
        cart.items.forEach(function(item) {
          var variantText = item.variant_title && item.variant_title !== 'Default Title' ? item.variant_title : '';
          /* Shopify CDN: trocar tamanho na URL ou usar param &width= */
          var imgSrc = item.image || '';
          if (imgSrc) {
            /* Tentar Shopify CDN format: adicionar width via query param */
            if (imgSrc.indexOf('cdn.shopify.com') > -1) {
              imgSrc = imgSrc.replace(/&?width=\d+/, '') + (imgSrc.indexOf('?') > -1 ? '&' : '?') + 'width=180';
            }
          }

          html += '<div class="cart-drawer-item">';
          if (imgSrc) {
            html += '<a href="' + item.url + '"><img src="' + imgSrc + '" alt="" class="cart-drawer-item__img"></a>';
          } else {
            html += '<a href="' + item.url + '"><div class="cart-drawer-item__img" style="background:#f5f5f5;display:flex;align-items:center;justify-content:center;"><svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="#ccc" stroke-width="1"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="M21 15l-5-5L5 21"/></svg></div></a>';
          }
          html += '<div class="cart-drawer-item__info">';
          html += '<a href="' + item.url + '" class="cart-drawer-item__title">' + item.product_title + '</a>';
          html += '<span class="cart-drawer-item__price">' + fmt(item.final_line_price) + '</span>';
          if (variantText) {
            html += '<span class="cart-drawer-item__variant">TAMANHO: ' + variantText + '</span>';
          }
          html += '<div class="cart-drawer-item__qty">';
          html += '<button class="cart-drawer-item__qty-btn" data-key="' + item.key + '" data-qty="' + (item.quantity - 1) + '" onclick="updateCartItem(this)">−</button>';
          html += '<span class="cart-drawer-item__qty-val">' + item.quantity + '</span>';
          html += '<button class="cart-drawer-item__qty-btn" data-key="' + item.key + '" data-qty="' + (item.quantity + 1) + '" onclick="updateCartItem(this)">+</button>';
          html += '</div></div>';
          html += '<button class="cart-drawer-item__remove" data-key="' + item.key + '" data-qty="0" onclick="updateCartItem(this)" aria-label="Remover">';
          html += '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 01-2 2H7a2 2 0 01-2-2V6m3 0V4a2 2 0 012-2h4a2 2 0 012 2v2"/></svg>';
          html += '</button></div>';
        });

        itemsContainer.innerHTML = html;
        totalEl.textContent = fmt(cart.total_price);
        footer.style.display = '';
        updateHeaderCount(cart.item_count);
      });
  }

  window.updateCartItem = function(btn) {
    var key = btn.getAttribute('data-key');
    var qty = parseInt(btn.getAttribute('data-qty'));
    btn.disabled = true;

    fetch('/cart/change.js', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ id: key, quantity: qty })
    })
    .then(function(r) { return r.json(); })
    .then(function() { renderCart(); })
    .catch(function() { btn.disabled = false; });
  };

  function updateHeaderCount(count) {
    var badges = document.querySelectorAll('.cart-count, [data-cart-count]');
    badges.forEach(function(el) {
      el.textContent = count;
      el.style.display = count > 0 ? '' : 'none';
    });
  }

  /* Carregar na abertura inicial se já tem itens */
  window.refreshCartDrawer = renderCart;

  /* ---- Cupom de desconto ---- */
  window.applyCoupon = function() {
    var input = document.getElementById('cart-coupon-input');
    var msg = document.getElementById('cart-coupon-msg');
    var btn = document.getElementById('cart-coupon-btn');
    var code = (input.value || '').trim();

    if (!code) {
      msg.textContent = 'Digite um código de cupom';
      msg.className = 'cart-drawer__coupon-msg error';
      return;
    }

    btn.disabled = true;
    btn.textContent = '...';

    /* Shopify aplica cupons via /discount/{code} — redireciona e salva no checkout */
    fetch('/discount/' + encodeURIComponent(code), { method: 'GET', redirect: 'follow' })
      .then(function() {
        msg.textContent = 'Cupom "' + code + '" será aplicado no checkout';
        msg.className = 'cart-drawer__coupon-msg success';
        input.value = '';
      })
      .catch(function() {
        msg.textContent = 'Não foi possível aplicar. Tente no checkout.';
        msg.className = 'cart-drawer__coupon-msg error';
      })
      .finally(function() {
        btn.disabled = false;
        btn.textContent = 'APLICAR';
      });
  };

  /* Enter no input aplica o cupom */
  document.getElementById('cart-coupon-input').addEventListener('keydown', function(e) {
    if (e.key === 'Enter') { e.preventDefault(); applyCoupon(); }
  });
})();
</script>
