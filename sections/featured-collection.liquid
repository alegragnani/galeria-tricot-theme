<!-- Featured Collection Section -->
<section class="featured-collection">
  <div class="featured-collection__header">
    {% if section.settings.title %}
      <h2 class="featured-collection__title">{{ section.settings.title }}</h2>
    {% endif %}
  </div>

  <div class="featured-collection__carousel" data-carousel>
    <div class="featured-collection__track">
      {% if section.settings.collection != blank %}
        {% for product in section.settings.collection.products limit: 12 %}
          <div class="featured-collection__item">
            {% render 'product-card', product: product %}
          </div>
        {% endfor %}
      {% endif %}
    </div>
  </div>
</section>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Italiana&family=Raleway:wght@300;400;500;600;700&display=swap');

  .featured-collection {
    padding: 1.5rem 2rem 4rem;
    position: relative;
    background: #ffffff;
  }

  .featured-collection__header {
    margin-bottom: 2rem;
    text-align: center;
  }

  .featured-collection__title {
    font-family: 'Italiana', 'Cormorant Garamond', Georgia, serif;
    font-size: 32px;
    font-weight: 400;
    text-transform: uppercase;
    letter-spacing: 0.6px;
    margin: 0;
    color: #BC8E49;
    line-height: 1.3;
  }

  .featured-collection__carousel {
    position: relative;
    overflow: hidden;
  }

  .featured-collection__track {
    display: flex;
    gap: 8px;
    overflow-x: auto;
    scroll-behavior: smooth;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    cursor: grab;
    user-select: none;
  }

  .featured-collection__track.is-dragging {
    cursor: grabbing;
    scroll-behavior: auto;
  }

  .featured-collection__track::-webkit-scrollbar {
    display: none;
  }

  .featured-collection__item {
    flex: 0 0 calc(25% - 6px);
    min-width: 200px;
    scroll-snap-align: start;
  }

  /* Tablet: 2 colunas (BP: grid--2-col-tablet-down) */
  @media (max-width: 1024px) {
    .featured-collection__item {
      flex: 0 0 calc(50% - 4px);
    }
  }

  /* Mobile: 2 produtos + peek do 3o (BP style) */
  @media (max-width: 749px) {
    .featured-collection {
      padding: 1rem 0 3rem;
    }

    .featured-collection__header {
      margin-bottom: 1.5rem;
      padding: 0 1rem;
    }

    .featured-collection__title {
      font-size: 26px;
    }

    .featured-collection__track {
      padding-left: 1rem;
    }

    .featured-collection__item {
      flex: 0 0 calc(44% - 4px);
      min-width: 140px;
    }

    /* Pseudo-elemento pra dar padding no final do scroll */
    .featured-collection__track::after {
      content: '';
      flex: 0 0 1rem;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    document.querySelectorAll('[data-carousel]').forEach(function(carousel) {
      const track = carousel.querySelector('.featured-collection__track');
      if (!track) return;

      let isDown = false;
      let startX = 0;
      let startScroll = 0;

      track.addEventListener('mousedown', function(e) {
        isDown = true;
        track.classList.add('is-dragging');
        startX = e.pageX - track.offsetLeft;
        startScroll = track.scrollLeft;
      });
      track.addEventListener('mouseleave', function() {
        isDown = false;
        track.classList.remove('is-dragging');
      });
      track.addEventListener('mouseup', function() {
        isDown = false;
        track.classList.remove('is-dragging');
      });
      track.addEventListener('mousemove', function(e) {
        if (!isDown) return;
        e.preventDefault();
        const x = e.pageX - track.offsetLeft;
        track.scrollLeft = startScroll - (x - startX);
      });
      // Evitar clicar num produto logo após arrastar
      track.addEventListener('click', function(e) {
        if (track.classList.contains('was-dragged')) {
          e.preventDefault();
          e.stopPropagation();
          track.classList.remove('was-dragged');
        }
      }, true);
      track.addEventListener('mousemove', function() {
        if (isDown) track.classList.add('was-dragged');
      });
    });
  });
</script>

{% schema %}
{
  "name": "Featured Collection",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "BEST SELLERS"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    }
  ],
  "presets": [
    {
      "name": "Featured Collection"
    }
  ]
}
{% endschema %}
