<!-- Dual Banner Section - Banner duplo lado a lado (estilo PatBO) -->
<section class="dual-banner">
  <div class="dual-banner__container">
    {% for block in section.blocks %}
      <a href="{{ block.settings.link }}" class="dual-banner__item" {{ block.shopify_attributes }}>
        <div class="dual-banner__image-wrapper">
          {% if block.settings.image %}
            <img
              src="{{ block.settings.image | image_url: width: 1200 }}"
              srcset="{{ block.settings.image | image_url: width: 600 }} 600w, {{ block.settings.image | image_url: width: 1200 }} 1200w, {{ block.settings.image | image_url: width: 1800 }} 1800w"
              sizes="(max-width: 768px) 100vw, 50vw"
              alt="{{ block.settings.heading | escape }}"
              class="dual-banner__image"
              loading="lazy"
              width="1200"
              height="900"
            >
          {% else %}
            <div class="dual-banner__placeholder">{{ 'image' | placeholder_svg_tag }}</div>
          {% endif %}
          <div class="dual-banner__overlay dual-banner__overlay--h-{{ block.settings.text_horizontal }} dual-banner__overlay--v-{{ block.settings.text_vertical }}">
            <div class="dual-banner__content" style="color: {{ block.settings.text_color }};">
              {% if block.settings.subheading != blank %}
                <p class="dual-banner__subheading" style="color: {{ block.settings.text_color }};">{{ block.settings.subheading }}</p>
              {% endif %}
              {% if block.settings.heading != blank %}
                <h3 class="dual-banner__heading" style="color: {{ block.settings.text_color }};">{{ block.settings.heading }}</h3>
              {% endif %}
              {% if block.settings.cta_text != blank %}
                <span class="dual-banner__cta" style="color: {{ block.settings.text_color }}; border-color: {{ block.settings.text_color }};">{{ block.settings.cta_text }}</span>
              {% endif %}
            </div>
          </div>
        </div>
      </a>
    {% endfor %}
  </div>
</section>

<style>
  .dual-banner {
    padding: 0;
    margin: 0;
    background-color: #ffffff;
  }

  .dual-banner__container {
    width: 100vw;
    max-width: 100vw;
    margin: 0;
    margin-left: calc(50% - 50vw);
    padding: 0;
    box-sizing: border-box;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0; /* TRAVADO: banners colados sem espaço — NÃO SEPARAR */
  }

  .dual-banner__item {
    display: block;
    text-decoration: none;
    color: inherit;
    overflow: hidden;
  }

  .dual-banner__image-wrapper {
    position: relative;
    width: 100%;
    aspect-ratio: 1 / 1;
    overflow: hidden;
    background-color: #f5f5f5;
  }

  .dual-banner__image,
  .dual-banner__placeholder {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .dual-banner__item:hover .dual-banner__image {
    transform: scale(1.04);
  }

  .dual-banner__overlay {
    position: absolute;
    inset: 0;
    display: flex;
    padding: 40px 24px;
    background: linear-gradient(180deg, rgba(0,0,0,0) 50%, rgba(0,0,0,0.35) 100%);
  }

  /* Horizontal */
  .dual-banner__overlay--h-left { justify-content: flex-start; text-align: left; }
  .dual-banner__overlay--h-center { justify-content: center; text-align: center; }
  .dual-banner__overlay--h-right { justify-content: flex-end; text-align: right; }

  /* Vertical */
  .dual-banner__overlay--v-top { align-items: flex-start; background: linear-gradient(180deg, rgba(0,0,0,0.35) 0%, rgba(0,0,0,0) 50%); }
  .dual-banner__overlay--v-middle { align-items: center; background: rgba(0,0,0,0.15); }
  .dual-banner__overlay--v-bottom { align-items: flex-end; }

  .dual-banner__content {
    color: #ffffff;
  }

  .dual-banner__subheading {
    font-family: 'Raleway', -apple-system, sans-serif;
    font-size: 12px;
    font-weight: 400;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    margin: 0 0 8px;
  }

  .dual-banner__heading {
    font-family: 'Italiana', 'Cormorant Garamond', Georgia, serif;
    font-size: 28px;
    font-weight: 400;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    margin: 0 0 16px;
    line-height: 1.3;
  }

  .dual-banner__cta {
    display: inline-block;
    font-family: 'Raleway', -apple-system, sans-serif;
    font-size: 12px;
    font-weight: 400;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: #ffffff;
    padding: 12px 28px;
    border: 1px solid #ffffff;
    transition: background-color 0.3s ease, color 0.3s ease;
  }

  .dual-banner__item:hover .dual-banner__cta {
    background-color: #ffffff;
    color: #545454;
  }

  @media (max-width: 768px) {
    .dual-banner {
      padding: 0;
    }
    .dual-banner__container {
      grid-template-columns: 1fr;
      gap: 0; /* TRAVADO: banners colados sem espaço — NÃO SEPARAR */
    }
    .dual-banner__heading {
      font-size: 20px;
    }
  }
</style>

{% schema %}
{
  "name": "Banner Duplo",
  "tag": "section",
  "class": "section-dual-banner",
  "settings": [],
  "blocks": [
    {
      "type": "banner",
      "name": "Banner",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Imagem (proporção 4:3 recomendada)"
        },
        {
          "type": "text",
          "id": "subheading",
          "label": "Texto pequeno (cima)",
          "default": "Confira"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Título principal",
          "default": "Nova Coleção"
        },
        {
          "type": "text",
          "id": "cta_text",
          "label": "Texto do botão",
          "default": "SHOP NOW"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        },
        {
          "type": "select",
          "id": "text_horizontal",
          "label": "Posição horizontal do texto",
          "options": [
            { "value": "left", "label": "Esquerda" },
            { "value": "center", "label": "Centro" },
            { "value": "right", "label": "Direita" }
          ],
          "default": "center"
        },
        {
          "type": "select",
          "id": "text_vertical",
          "label": "Posição vertical do texto",
          "options": [
            { "value": "top", "label": "Topo" },
            { "value": "middle", "label": "Meio" },
            { "value": "bottom", "label": "Embaixo" }
          ],
          "default": "bottom"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Cor do texto",
          "default": "#ffffff"
        }
      ]
    }
  ],
  "max_blocks": 2,
  "presets": [
    {
      "name": "Banner Duplo",
      "blocks": [
        { "type": "banner" },
        { "type": "banner" }
      ]
    }
  ]
}
{% endschema %}
