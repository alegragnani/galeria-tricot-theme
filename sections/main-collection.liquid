{% comment %}
  Collection page (PLP) — visual BP
  Breadcrumb > Título centralizado > Filtrar e Ordenar > Grid 4 colunas
{% endcomment %}

{% paginate collection.products by 48 %}

<div class="collection-page">
  {%- comment -%} Breadcrumb {%- endcomment -%}
  <nav class="collection-page__breadcrumb" aria-label="breadcrumb">
    <a href="/">Início</a>
    <span class="collection-page__breadcrumb-sep">›</span>
    <span aria-current="page">{{ collection.title }}</span>
  </nav>

  {%- comment -%} Título da coleção (centralizado, fonte Italiana = Cannia da BP) {%- endcomment -%}
  <h1 class="collection-page__title">{{ collection.title }}</h1>

  {%- comment -%} Filtrar e Ordenar {%- endcomment -%}
  <div class="collection-page__toolbar">
    <button class="collection-page__filter-btn" id="filter-toggle" aria-label="Abrir filtros">
      FILTRAR E ORDENAR
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <line x1="4" y1="6" x2="20" y2="6"></line>
        <line x1="4" y1="12" x2="20" y2="12"></line>
        <line x1="4" y1="18" x2="20" y2="18"></line>
        <circle cx="8" cy="6" r="2" fill="currentColor" stroke="none"></circle>
        <circle cx="16" cy="12" r="2" fill="currentColor" stroke="none"></circle>
        <circle cx="10" cy="18" r="2" fill="currentColor" stroke="none"></circle>
      </svg>
    </button>
  </div>

  {%- comment -%} Product Grid {%- endcomment -%}
  {% if collection.products_count > 0 %}
    <div class="collection-page__grid">
      {% for product in collection.products %}
        <div class="collection-page__item">
          {% render 'product-card', product: product %}
        </div>
      {% endfor %}
    </div>

    {%- comment -%} Pagination {%- endcomment -%}
    {% if paginate.pages > 1 %}
      <nav class="collection-page__pagination" aria-label="Paginação">
        {% if paginate.previous %}
          <a href="{{ paginate.previous.url }}" class="collection-page__page-link">‹</a>
        {% endif %}

        {% for part in paginate.parts %}
          {% if part.is_link %}
            <a href="{{ part.url }}" class="collection-page__page-link">{{ part.title }}</a>
          {% else %}
            {% if part.title == paginate.current_page %}
              <span class="collection-page__page-link collection-page__page-link--current">{{ part.title }}</span>
            {% else %}
              <span class="collection-page__page-link">{{ part.title }}</span>
            {% endif %}
          {% endif %}
        {% endfor %}

        {% if paginate.next %}
          <a href="{{ paginate.next.url }}" class="collection-page__page-link">›</a>
        {% endif %}
      </nav>
    {% endif %}
  {% else %}
    <div class="collection-page__empty">
      <p>Nenhum produto encontrado nesta coleção.</p>
    </div>
  {% endif %}
</div>

{%- comment -%} Filter Drawer (slide da direita) {%- endcomment -%}
<div class="filter-overlay" id="filter-overlay"></div>
<aside class="filter-drawer" id="filter-drawer" aria-label="Filtros">
  <div class="filter-drawer__header">
    <h2 class="filter-drawer__title">FILTRAR E ORDENAR</h2>
    <button class="filter-drawer__close" id="filter-close" aria-label="Fechar filtros">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
        <line x1="5" y1="5" x2="19" y2="19"></line>
        <line x1="19" y1="5" x2="5" y2="19"></line>
      </svg>
    </button>
  </div>

  <div class="filter-drawer__body">
    {%- comment -%} Ordenar {%- endcomment -%}
    <div class="filter-group">
      <button class="filter-group__toggle" data-filter-toggle>
        <span>ORDENAR</span>
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="6 9 12 15 18 9"></polyline></svg>
      </button>
      <div class="filter-group__body">
        <label class="filter-option"><input type="radio" name="sort_by" value="best-selling" checked> Mais vendidos</label>
        <label class="filter-option"><input type="radio" name="sort_by" value="created-descending"> Mais recentes</label>
        <label class="filter-option"><input type="radio" name="sort_by" value="price-ascending"> Menor preço</label>
        <label class="filter-option"><input type="radio" name="sort_by" value="price-descending"> Maior preço</label>
        <label class="filter-option"><input type="radio" name="sort_by" value="title-ascending"> A-Z</label>
      </div>
    </div>

    {%- comment -%} Filtro por disponibilidade {%- endcomment -%}
    <div class="filter-group">
      <button class="filter-group__toggle" data-filter-toggle>
        <span>DISPONIBILIDADE</span>
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="6 9 12 15 18 9"></polyline></svg>
      </button>
      <div class="filter-group__body">
        <label class="filter-option"><input type="checkbox" name="availability" value="in-stock"> Em estoque</label>
      </div>
    </div>

    {%- comment -%} Filtro por preço {%- endcomment -%}
    <div class="filter-group">
      <button class="filter-group__toggle" data-filter-toggle>
        <span>PREÇO</span>
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="6 9 12 15 18 9"></polyline></svg>
      </button>
      <div class="filter-group__body">
        <div class="filter-price-range">
          <input type="number" placeholder="Mín" class="filter-price-input" />
          <span>—</span>
          <input type="number" placeholder="Máx" class="filter-price-input" />
        </div>
      </div>
    </div>
  </div>

  <div class="filter-drawer__footer">
    <button class="filter-drawer__apply" id="filter-apply">APLICAR</button>
    <button class="filter-drawer__clear" id="filter-clear">Limpar filtros</button>
  </div>
</aside>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Italiana&family=Raleway:wght@300;400;500;600;700&display=swap');

  /* ============ COLLECTION PAGE — BP style ============ */
  .collection-page {
    max-width: 1600px;
    margin: 0 auto;
    padding: 0 50px 60px;
    font-family: 'Raleway', -apple-system, sans-serif;
    background: #ffffff;
  }

  /* Breadcrumb */
  .collection-page__breadcrumb {
    padding: 12px 0;
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0.4px;
    color: #545454;
  }
  .collection-page__breadcrumb a {
    color: #545454;
    text-decoration: none;
    transition: color 0.2s;
  }
  .collection-page__breadcrumb a:hover {
    color: #B3883D;
  }
  .collection-page__breadcrumb-sep {
    margin: 0 8px;
    color: #999;
  }

  /* Título da coleção — centralizado, Italiana (≈ Cannia da BP) */
  .collection-page__title {
    font-family: 'Italiana', 'Cormorant Garamond', Georgia, serif;
    font-size: 28px;
    font-weight: 400;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    color: #B3883D;
    text-align: center;
    margin: 10px 0;
    line-height: 1.3;
  }

  /* Toolbar: Filtrar e Ordenar — direita */
  .collection-page__toolbar {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    padding: 16px 0;
    margin-bottom: 8px;
  }
  .collection-page__filter-btn {
    display: flex;
    align-items: center;
    gap: 8px;
    background: none;
    border: none;
    font-family: 'Raleway', sans-serif;
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    color: #545454;
    cursor: pointer;
    padding: 8px 0;
    transition: color 0.2s;
  }
  .collection-page__filter-btn:hover {
    color: #B3883D;
  }

  /* Product Grid — 4 colunas desktop, gap 4px (BP medido) */
  .collection-page__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px 12px;
  }

  .collection-page__item {
    position: relative;
  }

  /* Pagination */
  .collection-page__pagination {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 4px;
    margin-top: 48px;
    padding: 24px 0;
  }
  .collection-page__page-link {
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 40px;
    height: 40px;
    padding: 0 12px;
    font-family: 'Raleway', sans-serif;
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0.3px;
    color: #545454;
    text-decoration: none;
    border: 1px solid transparent;
    transition: all 0.2s;
  }
  .collection-page__page-link:hover {
    border-color: #E0DCD4;
  }
  .collection-page__page-link--current {
    border-color: #545454;
    font-weight: 600;
  }

  /* Empty collection */
  .collection-page__empty {
    text-align: center;
    padding: 80px 0;
    color: #999;
    font-size: 16px;
  }

  /* ============ FILTER DRAWER ============ */
  .filter-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.4);
    z-index: 9998;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.35s ease;
  }
  .filter-overlay.is-open {
    opacity: 1;
    visibility: visible;
  }
  .filter-drawer {
    position: fixed;
    top: 0;
    right: 0;
    width: 380px;
    max-width: 90vw;
    height: 100vh;
    background: #ffffff;
    z-index: 9999;
    transform: translateX(100%);
    transition: transform 0.35s ease;
    display: flex;
    flex-direction: column;
    font-family: 'Raleway', sans-serif;
  }
  .filter-drawer.is-open {
    transform: translateX(0);
  }
  .filter-drawer__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 24px;
    border-bottom: 1px solid #E0DCD4;
  }
  .filter-drawer__title {
    font-family: 'Raleway', sans-serif;
    font-size: 14px;
    font-weight: 400;
    letter-spacing: 1px;
    margin: 0;
  }
  .filter-drawer__close {
    background: none;
    border: none;
    cursor: pointer;
    color: #545454;
    padding: 4px;
  }
  .filter-drawer__body {
    flex: 1;
    overflow-y: auto;
    padding: 0;
  }

  /* Filter groups (acordeão) */
  .filter-group {
    border-bottom: 1px solid #E0DCD4;
  }
  .filter-group__toggle {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    padding: 20px 24px;
    background: none;
    border: none;
    font-family: 'Raleway', sans-serif;
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0.8px;
    color: #545454;
    cursor: pointer;
  }
  .filter-group__toggle svg {
    transition: transform 0.3s ease;
  }
  .filter-group.is-open .filter-group__toggle svg {
    transform: rotate(180deg);
  }
  .filter-group__body {
    display: none;
    padding: 0 24px 20px;
  }
  .filter-group.is-open .filter-group__body {
    display: block;
  }
  .filter-option {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 0;
    font-size: 14px;
    font-weight: 400;
    color: #545454;
    cursor: pointer;
  }
  .filter-option input {
    width: 16px;
    height: 16px;
    cursor: pointer;
    accent-color: #B3883D;
  }
  .filter-price-range {
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .filter-price-input {
    flex: 1;
    padding: 10px 12px;
    border: 1px solid #E0DCD4;
    font-family: 'Raleway', sans-serif;
    font-size: 14px;
    outline: none;
  }
  .filter-price-input:focus {
    border-color: #B3883D;
  }

  .filter-drawer__footer {
    padding: 20px 24px;
    border-top: 1px solid #E0DCD4;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .filter-drawer__apply {
    width: 100%;
    padding: 14px;
    background: #2B1810;
    color: #ffffff;
    border: none;
    font-family: 'Raleway', sans-serif;
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 1px;
    cursor: pointer;
    transition: background 0.2s;
  }
  .filter-drawer__apply:hover {
    background: #1a0f09;
  }
  .filter-drawer__clear {
    width: 100%;
    padding: 10px;
    background: none;
    border: none;
    font-family: 'Raleway', sans-serif;
    font-size: 13px;
    font-weight: 400;
    color: #545454;
    text-decoration: underline;
    cursor: pointer;
  }

  /* ============ TABLET ============ */
  @media (max-width: 1024px) {
    .collection-page {
      padding: 0 30px 40px;
    }
    .collection-page__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  /* ============ MOBILE ============ */
  @media (max-width: 749px) {
    .collection-page {
      padding: 0 15px 32px;
    }
    .collection-page__title {
      font-size: 24px;
      margin: 8px 0;
    }
    .collection-page__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 20px 8px;
    }
    .collection-page__toolbar {
      padding: 12px 0;
      margin-bottom: 4px;
    }
    .filter-drawer {
      width: 100%;
      max-width: 100vw;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Filter drawer toggle
    var filterToggle = document.getElementById('filter-toggle');
    var filterClose = document.getElementById('filter-close');
    var filterDrawer = document.getElementById('filter-drawer');
    var filterOverlay = document.getElementById('filter-overlay');

    function openFilter() {
      filterDrawer.classList.add('is-open');
      filterOverlay.classList.add('is-open');
      document.body.style.overflow = 'hidden';
    }
    function closeFilter() {
      filterDrawer.classList.remove('is-open');
      filterOverlay.classList.remove('is-open');
      document.body.style.overflow = '';
    }

    if (filterToggle) filterToggle.addEventListener('click', openFilter);
    if (filterClose) filterClose.addEventListener('click', closeFilter);
    if (filterOverlay) filterOverlay.addEventListener('click', closeFilter);
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape') closeFilter();
    });

    // Filter group accordion
    document.querySelectorAll('[data-filter-toggle]').forEach(function(btn) {
      btn.addEventListener('click', function(e) {
        e.preventDefault();
        btn.closest('.filter-group').classList.toggle('is-open');
      });
    });

    // Sort functionality
    document.querySelectorAll('input[name="sort_by"]').forEach(function(radio) {
      radio.addEventListener('change', function() {
        var url = new URL(window.location);
        url.searchParams.set('sort_by', this.value);
        window.location.href = url.toString();
      });
    });

    // Apply / Clear
    var applyBtn = document.getElementById('filter-apply');
    var clearBtn = document.getElementById('filter-clear');
    if (applyBtn) applyBtn.addEventListener('click', closeFilter);
    if (clearBtn) clearBtn.addEventListener('click', function() {
      window.location.href = window.location.pathname;
    });
  });
</script>

{% endpaginate %}

{% schema %}
{
  "name": "Collection",
  "settings": [
    {
      "type": "text",
      "id": "products_per_page",
      "label": "Produtos por página",
      "default": "24"
    }
  ]
}
{% endschema %}
